<UserControl x:Class="Asignacion.Wpf.Views.Estudiante.MisCursosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Mis cursos" FontSize="18" FontWeight="Bold"/>
            <Button Content="Actualizar" Command="{Binding CargarCommand}" Padding="10,4" HorizontalAlignment="Right"/>
        </DockPanel>

        <TextBlock Grid.Row="1" Text="{Binding MensajeError}" Foreground="Red" Margin="0,0,0,8"/>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Cursos}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Período" Binding="{Binding PeriodoCodigo}"/>
                <DataGridTextColumn Header="Sección" Binding="{Binding SeccionCodigo}"/>
                <DataGridTextColumn Header="Curso" Binding="{Binding CursoNombre}" Width="*"/>
                <DataGridTextColumn Header="Créditos" Binding="{Binding Creditos}"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding EstadoDetalle}"/>
                <DataGridTextColumn Header="Nota" Binding="{Binding NotaFinal}"/>
                <DataGridTextColumn Header="Resultado" Binding="{Binding Resultado}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
