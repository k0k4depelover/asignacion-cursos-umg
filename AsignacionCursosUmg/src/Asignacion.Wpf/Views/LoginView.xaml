<UserControl x:Class="Asignacion.Wpf.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#FF1E293B">
        <Border Background="White" CornerRadius="8" Width="360" Height="360"
                HorizontalAlignment="Center" VerticalAlignment="Center" Padding="24">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Asignación de Cursos UMG" FontSize="18" FontWeight="Bold" Margin="0,0,0,4" HorizontalAlignment="Center"/>
                <TextBlock Text="Inicio de sesión" FontSize="13" Foreground="Gray" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                <TextBlock Text="Correo:" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Correo, UpdateSourceTrigger=PropertyChanged}" Padding="6" Margin="0,0,0,12"/>

                <TextBlock Text="Contraseña:" Margin="0,0,0,4"/>
                <PasswordBox x:Name="PasswordBox" Padding="6" Margin="0,0,0,8"
                             PasswordChanged="PasswordBox_OnPasswordChanged"/>

                <TextBlock Text="{Binding MensajeError}" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,8"/>

                <Button Content="Ingresar" Command="{Binding IniciarSesionCommand}" Padding="8" IsDefault="True"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
