<UserControl x:Class="Asignacion.Wpf.Views.Catedratico.MisSeccionesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Mis secciones" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" Text="{Binding MensajeError}" Foreground="Red" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Ver estudiantes" Command="{Binding VerEstudiantesCommand}" Padding="10,4" Margin="0,0,8,0"/>
            <Button Content="Actualizar" Command="{Binding CargarCommand}" Padding="10,4"/>
        </StackPanel>

        <DataGrid Grid.Row="3" ItemsSource="{Binding Secciones}" SelectedItem="{Binding Seleccionada}"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding Codigo}"/>
                <DataGridTextColumn Header="Curso" Binding="{Binding CursoNombre}" Width="*"/>
                <DataGridTextColumn Header="Período" Binding="{Binding PeriodoCodigo}"/>
                <DataGridTextColumn Header="Salón" Binding="{Binding SalonNombre}"/>
                <DataGridTextColumn Header="Cupo" Binding="{Binding CupoOcupado}"/>
                <DataGridTextColumn Header="Máx" Binding="{Binding CupoMaximo}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
