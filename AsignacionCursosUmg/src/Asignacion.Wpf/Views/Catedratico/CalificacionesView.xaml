<UserControl x:Class="Asignacion.Wpf.Views.Catedratico.CalificacionesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding SeccionCodigo, StringFormat='Calificaciones — sección {0}'}" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" Text="{Binding MensajeError}" Foreground="Red" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="2" Text="{Binding MensajeExito}" Foreground="Green" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Guardar cambios" Command="{Binding GuardarCambiosCommand}" Padding="10,4" Margin="0,0,8,0"/>
            <Button Content="Actualizar" Command="{Binding CargarCommand}" Padding="10,4"/>
        </StackPanel>

        <DataGrid Grid.Row="4" ItemsSource="{Binding Filas}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Carnet" Binding="{Binding EstudianteCarnet}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Estudiante" Binding="{Binding EstudianteNombre}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Resultado actual" Binding="{Binding ResultadoActual}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Nota final" Binding="{Binding NotaFinal, UpdateSourceTrigger=PropertyChanged}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
