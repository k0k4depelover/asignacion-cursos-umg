<UserControl x:Class="Asignacion.Wpf.Views.Admin.SimpleCrudListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Nuevo" Command="{Binding NuevoCommand}" Padding="10,4" Margin="0,0,6,0"/>
            <Button Content="Editar" Command="{Binding EditarCommand}" Padding="10,4" Margin="0,0,6,0"/>
            <Button Content="Desactivar/Activar" Command="{Binding EliminarCommand}" Padding="10,4" Margin="0,0,6,0"/>
            <Button Content="Actualizar" Command="{Binding CargarCommand}" Padding="10,4" Margin="0,0,16,0"/>
            <CheckBox Content="Incluir inactivos" IsChecked="{Binding IncluirInactivos}" VerticalAlignment="Center"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="{Binding MensajeError}" Foreground="Red" Margin="0,0,0,8"/>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Items}" SelectedItem="{Binding Selected}"
                  AutoGenerateColumns="True" IsReadOnly="True" CanUserAddRows="False"
                  SelectionMode="Single"/>
    </Grid>
</UserControl>
