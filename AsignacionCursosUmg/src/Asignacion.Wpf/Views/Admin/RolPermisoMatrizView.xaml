<UserControl x:Class="Asignacion.Wpf.Views.Admin.RolPermisoMatrizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Roles y permisos" FontSize="16" FontWeight="Bold"/>
            <Button Content="Actualizar" Command="{Binding CargarCommand}" Padding="10,4" HorizontalAlignment="Right"/>
        </DockPanel>

        <TextBlock Grid.Row="1" Text="{Binding MensajeError}" Foreground="Red" Margin="0,0,0,8"/>

        <DataGrid x:Name="Matriz" Grid.Row="2" ItemsSource="{Binding Filas}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Permiso" Binding="{Binding NombrePermiso}" IsReadOnly="True" Width="220"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
